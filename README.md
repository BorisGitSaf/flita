# flita
Для заданий по функциональной логике и теории алгоритмов
Сафонов ИУ4-23Б
1 сем - 1 вар

# Задание 1
В вашем распоряжении есть два множества A и B (можно a и b)
Изначально оба множества пустые. Элементами могут быть десятичные целые числа
Основные функции для множества A (для B аналогичны):
A - вывод элементов A
Аi - перезапись множества. После этого с новой строки ввести элементы через пробел
Ac - очищение множества
A+ x - добавление x в А
A- x - удаление x из A
Для выхода введите e или Esc
Для вызова этого сообщения - h или ?

Принцип работы: При запуске программы создаются два глобальных массива: 
ai и bi длиной 1000. Они могут содержать только элементы int Так как во 
множестве, в отличие от массивов, не может быть повторяющихся элементов 
и оно должно иметь динамическую длину, обозначением конца множества в 
массиве являются два повторяющихся элемента, они пренадлежат элементам 
конца массива. 
Изначально оба множества пустые и представляют собой массивы 
{0 0 0 ... 0}. При добавлении элемента он записывается в конец 
множества вместо первого элемента из элементов конца множества. То есть 
при A+ 1 ai = {1 0 0 ... 0}. Если требуется записать элемент равный 
элементам, обзначающим конец массива, элементы конца массива 
перезаписываются. То есть при A+ 0 ai = {1 0 1 1 ... 1}. Таким образом 
элементы конца массива никогда не совпадают с последним элементом 
множества. При удалении i-го элемента, все значения массива, начиная с 
i до 999 получают значения своего правого соседа. Если при добавлении 
элемента, он уже находится во множестве или при удалении элемент не 
находится во множестве, выводится соответствующее сообщение

# Задание 2
Программа создает визуальное представление графа из матрицы смежности. Читает входной файл, определяет размер матрицы по количеству пробелов в первой строке, затем анализирует матрицу смежности, пропуская нулевые значения. Генерирует описание графа в формате DOT, где вершины обозначаются буквами A, B, C..., а ребра содержат веса из матрицы. Для изолированных вершин создает отдельные узлы. Запускает утилиту dot для преобразования в PNG и автоматически открывает изображение.

# Задание 3
Расширенная версия первой программы с анализом связности графа. Выполняет те же функции, но дополнительно подсчитывает количество ребер и проверяет связность графа по теореме о минимальном количестве ребер. Если ребер больше (N-1)(N-2)/2 - граф связный, иначе выводит предупреждение о невозможности точного определения. Для связных графов добавляет суффикс "_связный" к имени выходного файла и выводит информацию о связности в консоль.
